# fail2ban-Filter für das V.A.U.L.T. HUD-Login.
# Passt auf die Zeilen, die der Orchestrator bei Fehl-Logins nach
# instance/auth.log schreibt: "... VAULT_AUTH_FAIL ip=<IP> user=..."
[Definition]
failregex = ^.*VAULT_AUTH_FAIL ip=<HOST> user=.*$
ignoreregex =
