# Instanz-Konfiguration (Betriebsmodus, Ports) – überlebt Git-Updates
instance/config.env

# Logs der Automationen
automations/*.log

# lokale Claude-Einstellungen
.claude/settings.local.json

# OS / Editor
.DS_Store

# Python / Node Build-Artefakte
__pycache__/
*.pyc
node_modules/

# Vault-Task-Ausgaben (Runner-Logs)
vault/runs/

# Obsidian: nur veränderlichen Workspace-State ignorieren, Config behalten
vault/.obsidian/workspace*.json
vault/.obsidian/cache

# Instanz-Stores (Tasks/Settings/Agents – lokal, mit API-Keys)
instance/tasks.json
instance/settings.json
instance/agents.json
instance/auth.json
instance/board.json

# Automatische Backups + Logs
backups/
instance/audit.log
instance/auth.log
instance/schedule.json
